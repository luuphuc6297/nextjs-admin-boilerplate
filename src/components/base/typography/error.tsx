import { FormHelperText } from '@mui/material'
import { styled } from '@mui/material/styles'

export const ErrorCaption = styled(FormHelperText)({
    // marginTop: '4px',

    lineHeight: '16px',
    fontWeight: 400,
    fontSize: '12px',
    fontFamily: 'Linik Sans',
})
